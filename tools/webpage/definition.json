{
  "type": "function",
  "function": {
    "name": "webpage",
    "description": "Get a web page, in format 'text' or 'raw' (defaults to 'text') ('raw' for HTML source)",
    "parameters": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "URL of the web page"
        },
        "format": {
          "enum": ["text", "raw"],
          "description": "Output format. 'text' for text-only version. 'raw' for HTML source.  Defaults to 'text'"
        }
      },
      "required": ["url"]
    }
  }
}
