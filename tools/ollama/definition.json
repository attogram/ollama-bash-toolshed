{
  "type": "function",
  "function": {
    "name": "ollama",
    "description": "interact with the Ollama application",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "description": "'models_list' to list all models in ollama, 'model_info' to get info about a model, 'ollama_version' to get current version of ollama",
          "type": "string",
          "enum": ["models_list", "model_info", "ollama_version"]
        },
        "model": {
          "type": "string",
          "description": "The model name. Required for action 'model_info'"
        }
      },
      "required": ["action"]
    }
  }
}
